 <%= form_for @reply,
	:url => new_reply_path,
	:html => { :style => "display:inline;" },
	:remote => true  do |f| %>
<%= f.hidden_field :user_id, :value => @user.id %>
<%= f.hidden_field :comment_id, :value => comment.id %>
<%= link_to 'Reply', "#", :onclick=>"$(this).submit()", :class => "comment_link", :remote => true %>
<% end %>
