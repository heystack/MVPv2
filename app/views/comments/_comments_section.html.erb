<div class="stk_section_header">Talk of the Town</div>
<div class="comment">
<%= form_for(@comment) do |f| %>
	<%= render 'comments/comments_form', :f => f %>
<% end %>
</div>
<% if @stack.comments? %>
	<%= render @comments %>
<% end %>
